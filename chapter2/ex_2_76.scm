; Explicit dispatch requires to change existing code when adding either new
; types or new operations. In contrast, data-directed style allows to add
; procedures by just put into the table. With message passing, no change to
; exsisting codes is needed to add new types of data, but when adding new
; operations.
