; A*B =  (a-ap, a+ap) * (b-bp', b+bp')
;     = (ab-ab(p+p'-pp'), ab+ab(p+p'+pp'))
;
; p and p' is a small number, so these product can be ignored.
; threfore, the percentage tolerance of A*B approximate the sum of p and p'.
; 
